rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Dokumen PPDB dan berkas santri hanya dilayani API server terautentikasi.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
